import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFFCFA224); // new brand gold
  static const Color black = Color(0xFF000000);

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color whiteshade = Color(0xFFF8F9FA);
  static const Color grayshade = Color(0xFFEBEBEB);
  static const Color blackshade = Color(0xFF555555);
  static const Color hintText = Color(0xFFC7C7CD);

  // Supporting colors (kept but aligned)
  static const Color blue = Color(0xFF758CC8); // replaced orange misuse
  static const Color lightblueshade = Color(0xFFA6C0FF);
  static const Color lightsky = Color(0xFFA6C0FF);

  // Optional accent (you can keep or remove)
  static const Color orange = Color(0xFFCFA224); // aligned to brand
  static const Color lightblue = Color(0xFFCFA224); // aligned to brand
}